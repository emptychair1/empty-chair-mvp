# M4 Operator Test Drive

Route: `/m4-operator`

This page is intentionally founder/operator-facing. It shows M4 reading a signed-in shop's current OPEN inventory, previewing her top consented-customer candidates, and then explicitly handing a selected opening to Empty Chair's existing guarded recovery engine.

The Activate button is consequential: it uses the real `start_recovery_campaign` path. The page therefore requires an explicit browser confirmation before activation.

The demo does not bypass calendar safety, communication consent, cooldown rules, sequential offers, claim logic, booking protections, or delivery safeguards.
