services:
  - type: web
    name: empty-chair
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn features:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
